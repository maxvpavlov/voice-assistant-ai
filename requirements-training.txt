# Training Requirements for Full Model

# Core ML/Training
torch>=2.0.0
torchinfo>=1.8.0
torchmetrics>=1.0.0
scipy>=1.10.0
pyyaml>=6.0

# OpenWakeWord data utilities
pronouncing>=0.2.0  # Required by openwakeword.data module
scikit-learn>=1.0.0  # Required for model training
audiomentations>=0.33.0  # Required by openwakeword.data for audio augmentation

# Audio Processing (already have these)
# openwakeword>=0.6.0
# numpy>=1.24.0
# pyaudio>=0.2.13

# Data Augmentation
# pyroomacoustics>=0.7.3   # For room impulse response simulation (optional)

# TTS for Synthetic Data Generation (optional, can use recorded samples)
# Will be added if needed
